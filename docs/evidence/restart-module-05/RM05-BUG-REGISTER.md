# RESTART MODULE 05 — BUG REGISTER

Every defect found, all by running the application rather than by reading it.

| ID | Requirement | Platform | Severity | Screen / API | Actual | Expected | Root cause | Fix | Retest | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **B05-01** | RM05-001 | Customer Web | **P0** | `/trips/plan` | A "coming later" placeholder. The whole backend existed and no browser could reach it | A working planner | Restart Module 02 created the route and left its content to this module | Built the planner, picker, hooks and API layer | Driven end to end in Chromium | **FIXED** |
| **B05-02** | RM05-007 | Customer Web | **P0** | `/profile` | No way to create or list a saved address in a browser, so the planner's mandatory Home/Work/Other could never appear | Saved places | Restart Module 04 was never run for web | Built `/profile/addresses`: list, create from a real place, delete | Two addresses created live and used as journey endpoints | **FIXED** |
| **B05-03** | RM05-043 | Customer Web | P1 | Home | Both CTAs linked to `/trips/undefined` and `/orders/undefined` | Links to the record | `useHome.ts` declared `uuid`; the server sends the public id as `id`. TypeScript could not catch it — the payload is a cast over `unknown` | Renamed the field; both links read `id` | Home CTA href is the real trip uuid | **FIXED** |
| **B05-04** | RM05-011 | Customer Web | P1 | Location picker | "Use my current location" span for ever when geolocation was blocked. Chromium called **neither** callback and did **not** honour its own `timeout` option | A message and a way out | The code trusted the browser's `timeout` as its backstop | A 15 s application-side watchdog with its own `no-response` reason | Recovered in 16 s with an actionable message; saved places and search still offered | **FIXED** |
| **B05-05** | RM05-011 | Customer Web | P2 | Location picker | The reverse-geocode call was made, paid for, and its answer discarded — the field still read "Current location" 2 km from a place the gazetteer knows | The place name | The fix resolved on the device coordinates first, which closed the picker and unmounted the hook before the answer arrived | Await the lookup, bounded at 3 s, then resolve once | Controlled fix 28.6129, 77.2295 → "New Delhi" | **FIXED** |
| **B05-06** | RM05-017 | Customer Web | **P1** | `usePlaceSearch` | An inline callback produced an infinite render loop. It never fired in the app (both callers happen to pass a memoised `signOut`) and it hung the entire test suite on the first `renderHook` | A hook that works for any caller | `onSessionOver` was an effect dependency, and the effect set state unconditionally | Hold the callback in a ref; only write `idle` when not already idle | 7 tests pass; the whole customer suite runs in 8 s | **FIXED** |
| **B05-07** | RM05-042 | Customer Web | P2 | Trips tab | A payload that was not a list threw during render and React unmounted the tree — a blank white tab, the failure only in the console | An empty tab | `.map` on a value typed as an array but cast from `unknown` | `Array.isArray` on all three list screens | The shell test that exposed it passes | **FIXED** |
| **B05-08** | RM05-048 | Backend | P2 | `api.request` log | `GET /customer/places/{place}` wrote a provider place id next to `actor_id` — a record of where a named customer was going | No place in an operational log | The middleware logs the concrete path; for this one route the path *is* a place | Redact private route-parameter values by value, so a url-encoded id cannot survive | Confirmed to fail without the fix and pass with it | **FIXED** |
| **B05-09** | RM05-007 | Customer Web | P2 | Saved places | `formatted_address` read "Green Park, New Delhi, Delhi 110016, New Delhi, Delhi 110016, IN" — every part twice | The address once | The screen sent the whole formatted address as `address_line_1`; the server composes `formatted_address` from line 1 plus city, state, postcode and country | Send the place's **name** as line 1 | Reads "Green Park, New Delhi, Delhi 110016, IN" | **FIXED** |
| **B05-10** | RM05-055 | Customer Web | P3 | Every page | A permanent `/favicon.ico` 404 in the live console | A clean console | No favicon and no `<link rel="icon">` | An SVG favicon | The error is gone — the control confirms the check was real | **FIXED** |
| **B05-11** | RM05-051 | Customer Web | P2 | Every page | Page headings clipped by the top of the viewport. Visible on Home too — nobody had looked at the top of a page before these screenshots | Breathing room | `.shell__main` had `padding-bottom` and no top or side padding | Full padding, set once in the shell | Re-captured at 1280 and 390 | **FIXED** |
| **B05-12** | RM05-052 | Customer Web | P2 | Planner CTA | The disabled "Plan journey" was white text on pale orange — roughly 2:1, and still read as a button to press. It is the state customers see most of the time on that screen | Clearly inactive, and legible | `opacity: 0.5` on a filled primary | A flat sunken surface with neutral-500 text — 4.76:1 | Re-captured | **FIXED** |

## Not fixed, and why

| ID | Finding | Why it stays open |
| --- | --- | --- |
| **B05-13** | `act(...)` warnings from `App.test.tsx` and `SignInScreen.test.tsx` | Pre-existing, from Restart Modules 02 and 03. Warnings, not failures, and outside this module's scope. Recorded rather than silently tidied |
| **B05-14** | Browser geolocation cannot work on the review deployment | It is plain http and browsers refuse. **MF-28**, blocked on **MF-16** — not a code defect |
