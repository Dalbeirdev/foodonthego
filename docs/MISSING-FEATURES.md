# MISSING FEATURES REGISTER

Restart Module 01. Commit `b49dcb4`.

Severity: **P0** breaks the core customer journey · **P1** important functionality
missing · **P2** UX or secondary · **P3** enhancement.

| ID | Feature | Platform | Module | Severity | Current state | Expected | Root cause | Required fix | Dependency | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ~~MF-01~~ | ~~All customer text invisible when `fonts.gstatic.com` is unreachable~~ **CLOSED by Restart Module 02** — the customer web app is React and renders real DOM text; zero external requests were made in every live capture. | Customer **Web** | 01 | **P1** | `fontFamily` is `null`, no font is bundled, so Flutter's CanvasKit renderer fetches Roboto from Google's CDN at runtime. Blocked CDN → every string renders blank. No error, no fallback. | Text renders from a font shipped with the build | No font asset in `pubspec.yaml`; reliance on Flutter's automatic remote fallback | Bundle the brand font under `mobile/assets/fonts/` and set `FotgTypography.fontFamily` | none | **OPEN** |
| ~~MF-02~~ | ~~Desktop layout stretches the phone UI edge to edge~~ **CLOSED by Restart Module 02** — 68rem capped container, rail above 900px, bottom bar below; all eight breakpoints captured. | Customer **Web** | 01 | **P2** | At 1440 and 1920 the primary CTA spans the full viewport width. Exactly the "do not stretch mobile UI to 1920px" case. | A max-width container, or a split/two-column layout at ≥1024 | Flutter layout written mobile-first with no width breakpoints | Introduce a responsive container and breakpoint policy in the shared shell | none | **CLOSED** |
| MF-03 | ETA engine | Web, Android, iOS | 18 | P1 | Not started. No route, no screen, no endpoint, no service. | Estimated arrival, food-ready estimate, timing alignment | Module 18 was never begun; no specification was ever supplied | Specify then build Module 18 | Module 18 spec | **NOT YET SCHEDULED** |
| MF-04 | Staff/operator sign-in | Restaurant Web, Admin Web | — | **P0** for operators | No endpoint mints a token for the 6 non-customer roles. Both dashboards render a hard-coded `DEV_USER` persona. Anyone with the URL sees everything. | Real authenticated sessions, role-scoped | Deliberately deferred — `backend/routes/api.php:431` states the isolation was built before the login | Build staff authentication | none | **MISSING** |
| MF-05 | Restaurant dashboard — all 11 areas | Restaurant Web | — | **P0** for operators | `implemented: true` appears **0 times** in `web/apps/restaurant/src/navigation.tsx`. Every route renders `ModulePlaceholder`. No API call is made. | Orders, Availability, Menu, Restaurant, Staff, Earnings, Reports, Reviews, Support, Settings | Modules 02–17 built the customer vertical only | Build per area, after MF-04 | MF-04 | **MISSING** |
| MF-06 | Admin panel — 13 of 14 areas | Admin Web | — | P1 | Only System Health is implemented. The other 13 render `ModulePlaceholder`. | Customers, Restaurants, Routes, Orders, Payments, Promotions, Reviews, Support, Analytics, Administration, Settings, Audit Logs | As MF-05 | Build per area, after MF-04 | MF-04 | **MISSING** |
| MF-07 | Restaurant tenant endpoints unreachable from any UI | Restaurant Web | 14T | P1 | 7 endpoints exist under `/api/v1/restaurant/*`, are tenant-scoped and covered by 22 cross-tenant security tests — and **no frontend calls them**. | The dashboard reads its own restaurant, menu and orders | No operator UI was built | Wire the dashboard to them | MF-04 | **BACKEND ONLY** |
| MF-08 | Map tiles | Web, Android, iOS | 06 | P1 | No `com.google.android.geo.API_KEY` in the manifest and no iOS equivalent. Tiles will not load on any platform. | A rendered map on the Route and Discovery screens | No Google Maps API key exists; one cannot be fabricated — it is a billable credential on the client's account | Client supplies a key with Maps SDK for Android + iOS enabled | client | **BLOCKED** |
| MF-09 | Real SMS delivery | all | 03 | P1 | `OTP_PROVIDER=log` writes codes to a file. `TwilioOtpProvider` is built and tested but has never sent a message. | An SMS arrives on the handset | No Twilio credentials and no DLT registration | Client supplies API key + Messaging Service; register DLT template | client | **BLOCKED** |
| MF-10 | Live payment capture | all | 15 | P1 | Razorpay adapter built, signature verification and webhooks tested, exercised only through a deterministic fake. **No payment has ever been taken.** | A real captured payment | No Razorpay credentials | Client supplies keys | client | **BLOCKED** |
| MF-11 | Order state cannot advance past PLACED | all | 17 | P1 | The transition service, matrix and history table exist and are tested. Nothing operator-facing calls them, so a review order stays at PLACED forever. | A restaurant moves an order ACCEPTED → COOKING → READY → PICKED_UP | The restaurant dashboard does not exist | Restaurant order screen | MF-04, MF-05 | **BACKEND ONLY** |
| MF-12 | Live routing-provider figures | all | 06, 07 | P1 | `DevelopmentRouteProvider` computes geometry locally. Detour and travel-time figures are not from a live traffic-aware provider. | Real traffic-aware durations and detours | No routing provider credentials | Client supplies a routing provider | client | **BLOCKED** |
| MF-13 | Android live inspection by a person | Android | 01 | P2 | No Android SDK or emulator in this environment (KI-001). CI runs the release APK on a Pixel 6 emulator. | A screenshot from a running handset | Environment | Run on a device, or rely on CI artefacts | none | **IOS/ANDROID VERIFICATION PENDING** |
| MF-14 | iOS live inspection by a person | iOS | 01 | P2 | No Apple hardware reachable. CI simulator green. | A screenshot from a simulator or device | Environment | macOS runner or a Mac | none | **IOS TEST PENDING** |
| MF-15 | Installable iOS build | iOS | 01 | P1 | CI builds `--no-codesign`; the output installs on nothing. | A signed `.ipa` for TestFlight or ad-hoc | No Apple Developer account, certificate or provisioning profile | Client enrols and supplies signing assets | client | **BLOCKED** |
| MF-16 | HTTPS on the review deployment | all | — | P1 | Served on `http://techpio.tech:8080`. One-time codes and session tokens travel in clear text. | TLS | Port 443 on that host belongs to PioDesk, which must not be touched | Cloudflare Tunnel (chosen, not yet actioned) | client | **OPEN** |


## Added by Restart Module 02

| ID | Feature | Platform | Module | Severity | Current state | Required fix | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ~~MF-17~~ | ~~Web sign-in (phone + OTP)~~ **CLOSED by Restart Module 03** — verified live for a new and an existing customer. | Customer Web | R-03 | **P0** for web | `/login` exists and is honest, but a customer cannot sign in through the browser. The session store, guard and bearer-token client are built and working; only the OTP screens are missing. | Build the phone and code screens against the existing endpoints | **CLOSED** |
| MF-18 | Web session persists only for the tab | Customer Web | R-03 | P2 | The token is in `sessionStorage`, so closing the tab signs the customer out. Deliberate for now — the safer default before a refresh-token flow exists. | Decided in Restart Module 03: sessionStorage stays, as the safer default until a refresh-token flow exists. | **CLOSED — decided** |

| MF-19 | Suspended and disabled accounts not driven through a browser | Customer Web | R-03 | P2 | Backend tests prove a suspended customer cannot sign in with a correct code. No suspended fixture exists locally, so the customer-facing message was never seen on screen. | Seed a suspended fixture and capture the screen | **OPEN** |
| MF-20 | Android and iOS auth not inspected by a person | Android, iOS | R-03 | P2 | Unchanged by this module; 29 on-device integration tests run in CI. No SDK, emulator or Apple hardware is reachable from this environment. | Run on a device | **PENDING — environment** |

## Added or closed by Restart Module 05

| ID | Feature | Platform | Module | Severity | Current state | Required fix | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ~~MF-21~~ | ~~Trip planner absent from Customer Web~~ **CLOSED by Restart Module 05** — planner, picker, saved places, place search, current location, validation, create and route handoff all driven live in a browser. | Customer Web | R-05 | **P0** for web | `/trips/plan` rendered a "coming later" placeholder. The whole backend existed and nothing in a browser could reach it. | Build it | **CLOSED** |
| ~~MF-22~~ | ~~Saved addresses absent from Customer Web~~ **CLOSED by Restart Module 05**, in the minimum shape the planner needs: list, create from a real place, delete. | Customer Web | R-04 | **P0** for the planner | Module 04's API was complete and tested; no browser screen created or listed an address, so the planner's mandatory Home/Work/Other could never appear. | Build list + create + delete | **CLOSED** |
| ~~MF-23~~ | ~~Home CTAs linked to `/trips/undefined` and `/orders/undefined`~~ **CLOSED by Restart Module 05**. | Customer Web | R-02 | P1 | `useHome.ts` declared `uuid` on the active trip and order; the server sends the public id under `id`. Invisible until a journey existed to click. | Rename the field; link from `id` | **CLOSED** |
| ~~MF-24~~ | ~~"Use my current location" could hang for ever~~ **CLOSED by Restart Module 05**. | Customer Web | R-05 | P1 | When geolocation is blocked for the origin, Chromium calls neither callback *and* does not honour its own `timeout` option. The button span indefinitely; the only way out was a page reload. | An application-side watchdog, independent of the browser calling back | **CLOSED** |
| ~~MF-25~~ | ~~Place identifiers written to the request log~~ **CLOSED by Restart Module 05**. | Backend | R-05 | P2 | `api.request` logged the concrete path, so `GET /customer/places/{place}` wrote a provider place id next to `actor_id` — a record of where a named customer was going. Search queries and request bodies were never logged. | Redact private route-parameter values from the logged path | **CLOSED** |
| MF-26 | Rest of Restart Module 04 on Customer Web | Customer Web | R-04 | P1 | Restart Module 04 was never run. Profile name/phone/email are not editable in a browser, saved addresses cannot be edited, and the default address cannot be changed. Only what Restart Module 05 required was built. | Run Restart Module 04 for Customer Web | **PARTIAL — planner needs met, rest missing** |
| MF-27 | Trip planner not inspected by a person on Android or iOS | Android, iOS | R-05 | P2 | The Flutter planner, place search and location service exist in source and are covered by unit and widget tests. No Android SDK, emulator or Apple hardware is reachable from this environment, so no screen was looked at. | Run on a device | **PENDING — environment** |
| MF-28 | Browser geolocation unavailable on the review deployment | Customer Web | R-05 | P1 | Browsers only expose `navigator.geolocation` in a secure context. The review site is `http://techpio.tech:8080`, so "Use my current location" will report the insecure-context message there for every visitor. Saved places and place search are unaffected. | TLS on the review deployment — the same fix as MF-16 | **BLOCKED on MF-16** |
| MF-29 | Live Places provider never exercised | Web, Android, iOS | R-05 | P1 | `PLACES_PROVIDER=development` serves a fixed gazetteer of twelve real places at their real published coordinates. `GooglePlacesProvider` is built and unit-tested and has never made a live call. Session tokens, quota errors and key restrictions are therefore untested against Google itself. | Client supplies a Places API key; set `PLACES_PROVIDER=google` | **BLOCKED — client** |

## Added or closed by Restart Module 06

| ID | Feature | Platform | Module | Severity | Current state | Required fix | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ~~MF-31~~ | ~~Route screen absent from Customer Web~~ **CLOSED by Restart Module 06** — map shape, markers, distance, travel time, freshness, refresh, selection and the Module 07 handoff, all driven live. | Customer Web | R-06 | **P0** for web | `/trips/{id}` said "Route not calculated yet" whatever the route status was — including for a trip whose route was **READY** and selected in the database. | Build it | **CLOSED** |
| ~~MF-32~~ | ~~A Maps API key had no path into Android or iOS~~ **CLOSED by Restart Module 06.** | Android, iOS | R-06 | **P0** once a key exists | `MapsConfig` turned the map on from a `--dart-define`; the Android SDK reads its key from a manifest element that did not exist, and `GMSServices.provideAPIKey` was never called. Supplying a key would have produced a blank Android map and an iOS throw. | Gradle placeholder + manifest element; `provideAPIKey` from Info.plist; a guard test that reads the build files | **CLOSED** |
| ~~MF-33~~ | ~~Route error codes fell through to the generic message~~ **CLOSED by Restart Module 06.** | Customer Web | R-06 | P2 | The customer-safe allow-list predated the route codes, so a real 503 carrying "We could not work out a route right now." was replaced by "Something went wrong." Found by driving the provider-error state in a browser. | Read each route code, then add it | **CLOSED** |
| ~~MF-34~~ | ~~The map shrank as the screen grew~~ **CLOSED by Restart Module 06.** | Customer Web | R-06 | P2 | The split layout started at 1024, where the rail left the map a 430px column — narrower than the 710px it gets at 768. Only visible by measuring all eight widths. | Split at 1200; widen the container above it | **CLOSED** |
| MF-30 | No offline route cache | Customer Web, Android, iOS | R-06 | P3 | Losing the network on the route screen shows an honest connection message and a retry. A previously calculated route is not served from local storage. The brief permits this ("may display"), so it is recorded rather than claimed. | Cache the last route payload per trip, with a visible "last updated" | **NOT IMPLEMENTED — by decision** |
| MF-35 | Route alternatives never seen live | all | R-06 | P2 | The development routing provider returns exactly one route and must not fabricate a second. Selection between alternatives is covered by component tests with fixture data and by backend tests; no live run has ever shown two routes. | A routing credential — same blocker as MF-12 | **BLOCKED on MF-12** |
| MF-36 | The Google Maps browser path has never executed | Customer Web | R-06 | P1 | `RouteMap.tsx` loads the Maps JS API when `VITE_GOOGLE_MAPS_BROWSER_KEY` is set. No key exists and this environment cannot reach Google, so that branch has never run. The no-tiles state is what every screenshot shows. | A restricted browser Maps key | **BLOCKED — client** |
